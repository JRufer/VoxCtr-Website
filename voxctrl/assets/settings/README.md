# Settings captures

The settings gallery on the landing page (`#settings`) tabs between these:

| File                   | Settings tab       |
|------------------------|--------------------|
| `general.png`          | General            |
| `output-commands.png`  | Output Commands    |

Whole-window captures, same as the setup wizard shots. The stage is
`aspect-ratio: 872 / 1024`; change `.sg-stage` in `css/wizard.css` if yours differ.
